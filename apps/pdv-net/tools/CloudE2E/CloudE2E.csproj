<Project Sdk="Microsoft.NET.Sdk">
  <!-- O PDV em C# contra a retaguarda de verdade: ativa, vende, sincroniza. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\Pdv.Data\Pdv.Data.csproj" />
  </ItemGroup>
</Project>
